{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ perfilUsuario.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ perfilUsuario.email }}</td>
            </tr>
            <tr>
                <th>IsVerified</th>
                <td>{{ perfilUsuario.isVerified ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_index') }}">back to list</a>

    <a href="{{ path('app_user_edit', {'id': perfilUsuario.id}) }}">edit</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
