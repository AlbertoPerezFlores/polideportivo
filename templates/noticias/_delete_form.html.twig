<form method="post" action="{{ path('app_noticias_delete', {'id': noticia.id}) }}" onsubmit="return confirm('¿Estas seguro que deseas eliminar esta noticia?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ noticia.id) }}">
    <button class="btn btn-danger btn-block"><i class="fa fa-trash" style="font-size:24px"></i></button>
</form>
